<!DOCTYPE html>
<html lang="ro">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof pageTitle !== 'undefined' ? pageTitle : 'Magazin Mobilă' %></title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <header class="main-header">
        <nav class="container">
            <a href="/" class="nav-brand">Mobilă Acasă</a>
            <ul>
                <li><a href="/">Magazin</a></li>
                <% if (isLoggedIn) { %>
                    <li><a href="/cart">Coș</a></li>
                    <li><a href="/orders">Comenzi</a></li>
                    <% if (currentUser && currentUser.isAdmin) { %>
                        <li><a href="/admin/products">Admin</a></li>
                    <% } %>
                    <li>
                        <form action="/logout" method="POST">
                            <button type="submit" class="nav-logout-btn">Logout</button>
                        </form>
                    </li>
                <% } else { %>
                    <li><a href="/login">Login</a></li>
                    <li><a href="/register">Înregistrare</a></li>
                <% } %>
            </ul>
        </nav>
    </header>
    <main class="container page-content">